---
nodes:
  - name: towera
    role: tower
    app_name: tower_clustered
    instance_type: t2.large
    image: "{{ rhel7_ami_id|default('ami-85241def') }}"
    volumes:
      - device_name: /dev/sda1
        volume_type: gp2
        volume_size: 20
        delete_on_termination: true
  - name: towerb
    role: tower_east
    app_name: tower_clustered
    instance_type: t2.large
    image: "{{ rhel7_ami_id|default('ami-85241def') }}"
    volumes:
      - device_name: /dev/sda1
        volume_type: gp2
        volume_size: 20
        delete_on_termination: true
  - name: towerc
    role: tower_east
    app_name: tower_clustered
    instance_type: t2.large
    image: "{{ rhel7_ami_id|default('ami-85241def') }}"
    volumes:
      - device_name: /dev/sda1
        volume_type: gp2
        volume_size: 20
        delete_on_termination: true
  - name: towerdb
    role: towerdb
    app_name: tower_clustered
    instance_type: t2.large
    image: "{{ rhel7_ami_id|default('ami-85241def') }}"
    volumes:
      - device_name: /dev/sda1
        volume_type: gp2
        volume_size: 20
        delete_on_termination: true